jurisdictions:
  - USA
  - INDIA
# Maintainer-declared source-data recency (Note 14/15). The LLM cannot infer this
# from row values (GR-1), so it is declared here and copied into the snapshot
# manifest. Synthetic demo data — representative value.
data_as_of: "2025-12-31"
rule_refresh: online_preferred
conflict_policy: strictest_wins
approval:
  mode: hybrid
  max_synthetic_attempts: 5
parallelism:
  mode: auto
kanon_publish_gate:
  enabled: true
  quasi_identifiers:
    - AGE
    - SEX
  k_threshold: 5
